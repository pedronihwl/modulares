generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

model Video {
  id           String   @id
  title        String
  description  String
  url          String
  sizeInKb     Int
  duration     Int
  thumbnailUrl String?
  createdAt    DateTime
  updatedAt    DateTime
}
